# pyobjus-firebase
Objective-C bridges that expose Firebase's block-based iOS SDKs to pyobjus, letting Kivy/kivy-ios apps use FIRAuth, Google Sign-In, Firestore, Realtime Database, Cloud Functions, App Check, and more directly from Python — no Swift or Xcode-side glue code required beyond dropping in the bridge files.
